name: Claude PR Review
on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: claude-pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    if: >-
      github.event.pull_request.base.ref != 'stage' &&
      github.event.pull_request.base.ref != 'master' &&
      !startsWith(github.event.pull_request.base.ref, 'release') &&
      !contains(toJSON(github.event.pull_request.labels.*.name), 'sync')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Log review start
        run: |
          echo "========================================="
          echo "🔍 Claude PR Review 시작"
          echo "========================================="
          echo "📌 PR: #${{ github.event.pull_request.number }}"
          echo "📝 제목: ${{ github.event.pull_request.title }}"
          echo "👤 작성자: ${{ github.event.pull_request.user.login }}"
          echo "🔀 브랜치: ${{ github.head_ref }} → ${{ github.base_ref }}"
          echo "📊 변경: +${{ github.event.pull_request.additions }} / -${{ github.event.pull_request.deletions }}"
          echo "========================================="

      - name: Run Claude PR Review (full)
        if: github.event.action != 'synchronize'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          show_full_output: true
          prompt: |
            PR #${{ github.event.pull_request.number }}의 변경사항을 리뷰합니다.
            - 제목: ${{ github.event.pull_request.title }}
            - 브랜치: ${{ github.head_ref }} → ${{ github.base_ref }}
            - 라벨: ${{ join(github.event.pull_request.labels.*.name, ', ') }}

            `.claude/rules/pr-review.md`의 리뷰 실행 순서를 반드시 따르세요. `{PR_NUMBER}`는 `${{ github.event.pull_request.number }}`입니다.
          claude_args: |
            --allowedTools "Read,Glob,Grep,mcp__github_inline_comment__create_inline_comment,Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh api:*),Bash(gh pr comment:*)"

      - name: Run Claude PR Review (synchronize)
        if: github.event.action == 'synchronize'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          show_full_output: true
          prompt: |
            PR #${{ github.event.pull_request.number }}에 새로운 커밋이 push되었습니다.
            - 제목: ${{ github.event.pull_request.title }}
            - 브랜치: ${{ github.head_ref }} → ${{ github.base_ref }}
            - 라벨: ${{ join(github.event.pull_request.labels.*.name, ', ') }}

            `.claude/rules/pr-review.md`의 리뷰 실행 순서를 반드시 따르세요. `{PR_NUMBER}`는 `${{ github.event.pull_request.number }}`입니다.
            단, 정보 수집 단계에서 `gh pr diff` 대신 `git diff ${{ github.event.before }}..${{ github.event.after }} -- . ':!pnpm-lock.yaml' ':!*.d.ts' ':!.github/'`으로 incremental 변경사항만 조회하고, **해당 범위만** 리뷰합니다.
          claude_args: |
            --max-turns 10
            --allowedTools "Read,Glob,Grep,mcp__github_inline_comment__create_inline_comment,Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh api:*),Bash(git diff:*),Bash(gh pr comment:*)"

      - name: Log review complete
        if: always()
        run: |
          echo "========================================="
          echo "✅ Claude PR Review 완료"
          echo "========================================="
